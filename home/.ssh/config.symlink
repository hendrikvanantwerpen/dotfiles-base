## ~/.ssh/config

## Global settings, that cannot be overriden
Include config.global

## Include user specific settings first
Include config.user

## Include default settings, which will only apply if those settings
## were not set by host or global settings first
Host *
Include config.default
